import data from '../database/data.js';


export const getProyectos = (req, res) => {
    console log (data);
    res.status (200).json({
        status:200;
        message: 'Proyectos: ${message: "No hay proyectos"}',
    });
    status (404).json({message: data.lenght});
    }
